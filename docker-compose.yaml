version: '3.8'

services:
  frontend:
    image: lapin-admin
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "3002:80"
    pull_policy: build
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
      - ./dist:/usr/share/nginx/html
    restart: always